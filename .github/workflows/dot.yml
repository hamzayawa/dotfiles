name: CI

on:
  - push

jobs:
  build:
    runs-on: ubuntu-latest

  steps:
    - name: Run a one-line script
      uses: actions/checkout@v3
      run: echo Assalamu Aliakum

    - name: Run a multi-line script
      run: |
        echo Hello,
        echo world!
